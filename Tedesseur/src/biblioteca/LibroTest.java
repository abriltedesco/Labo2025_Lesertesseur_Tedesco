/*package biblioteca;
import static junit.framework.Assert.assertEquals;

import org.junit.Test;
import fecha.Fecha;

public class LibroTest {

    @Test
    public void testCompararFechas(){
        Fecha fecha = new Fecha(20, 12, 2007);
        Fecha fecha2 = new Fecha(1, 11, 2001);
        Libro libro1 = new Libro();
        if(fecha.getDia() < libro1.getFechaPublicacion().getDia() || fecha.getMes() < libro1.getFechaPublicacion().getMes()){
            System.out.println("el libro es mas antiguo");
        }

        assertEquals(1, fecha.getDia());
    }
    
}
*/